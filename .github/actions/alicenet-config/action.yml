name: 'Configure the environment for alicenet'
description: 'Configure the environment for alicenet'
runs:
  using: "composite"
  steps:
    - name: "Install abigen dependency"
      shell: bash
      run: go install github.com/ethereum/go-ethereum/cmd/abigen@v1.10.8
